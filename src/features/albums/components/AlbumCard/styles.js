import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  albumInfo: {
    marginHorizontal: 40,
  },
  albumName: {
    fontSize: 32,
  },
  container: {
    flexDirection: 'row',
    marginVertical: 12,
  },
  image: {
    height: 64,
    width: 64,
  },
});
